# Cargo / Tauri build output
src-tauri/target/
src-tauri/Cargo.lock

# jlinked JRE + staged server (regenerated by build.ps1)
runtime/

# Tauri frontend placeholder (we don't use a JS frontend — server serves SPA)
frontend-placeholder/

# Local signing material — never commit cert thumbprints or keys
.signing.env
